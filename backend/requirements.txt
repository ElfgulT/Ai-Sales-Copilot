# Çalışma zamanı bağımlılıkları (production)
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# --- Scraping (Sprint 2) ---
httpx>=0.27,<0.28          # async HTTP istemcisi (statik scraping + robots.txt)
beautifulsoup4>=4.12,<5.0  # HTML ayrıştırma/temizleme
brotli>=1.1,<2.0           # 'br' içerik kodlamasını çöz (aksi halde bozuk metin)
playwright>=1.47,<2.0      # dinamik (JS) scraping. Tarayıcı: `playwright install chromium`

# --- LLM (Sprint 3-4) ---
anthropic>=0.40           # Claude API resmi SDK'sı (AsyncAnthropic)
# SÜRÜM SABİTLİ: 1.2.0'da ThinkingConfig.thinking_budget kaldırıldı ve kod kırıldı.
# Yükseltmeden önce ThinkingConfig alanlarının değişip değişmediğini doğrulayın.
google-genai>=1.2,<2.0    # Gemini API (Google AI Studio, ücretsiz kota)

# --- RAG & Vektör Arama (Sprint 3) ---
scikit-learn>=1.5          # TF-IDF & Cosine Similarity yerel vektör indeksleme

# --- Auth & Veritabanı ---
sqlalchemy>=2.0,<3.0      # async ORM (SQLite için aiosqlite sürücüsüyle)
aiosqlite>=0.20,<1.0      # SQLAlchemy async SQLite sürücüsü
bcrypt>=4.0               # şifre hashleme (passlib yerine doğrudan kullanılır)
PyJWT>=2.9,<3.0           # JWT üretimi ve doğrulaması

# --- Önbellek ---
cachetools>=5.5,<6.0      # TTLCache — /analyze sonuçları önbellekleme

# --- E-posta doğrulama ---
email-validator>=2.1,<3.0 # Pydantic EmailStr için gerekli
